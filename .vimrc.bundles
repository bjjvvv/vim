filetype off " 暂时关闭

set rtp+=~/.vim/bundle/Vundle.vim/

if has("win32")
	let path='~/.vim/bundle'
	call vundle#begin(path)
else
	call vundle#begin()
endif

Bundle "tomasr/molokai"


call vundle#end()

filetype plugin on
